<%= simple_form_for(@query, :html => {:class => 'form-vertical' }) do |f| %>
  <%= f.label :name %>
  <%= f.text_field(:name) %>
  
  <%= f.label :description %>
  <%= f.text_area(:description, :class => "form-control", :rows => 5) %>

  <%= f.label :categories %>
  <%= f.autocomplete_field :tag_list, autocomplete_tag_name_queries_path, :"data-delimiter" => ', ' %>
  
  <br />
  
  <legend>Ontologies</legend>
  <div class="control-group">
    <% Ontology.all.each do |ont| %>
      <%= check_box_tag("query[ontology_ids][]", ont.id, @query.ontology_ids.include?(ont.id)) %> <%= ont.short_name %>
      <br />
    <% end %>
  </div>
  
  <br />
  <%= f.submit %>
  
<% end %>