class QueryCreator
  
  # takes a pattern, returns a query string
  def create_query(pattern, repository)
    raise "implement create_query(pattern, repository) for #{self.class.name} to do just that"
  end
  
end